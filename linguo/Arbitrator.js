const abi = require('../abis/Arbitrator.json');

module.exports = {
  contractName: 'Arbitrator',
  abi,
  /**
   * Arbitrator is just an interface, it does not have deployed instances.
   */
  networks: {},
};
